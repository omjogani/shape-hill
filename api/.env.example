# Supabase → Project Settings → Database → Connection string → URI (Direct connection).
# Must be port 5432. The 6543 pooler will not work for migrations.
DATABASE_URL=postgresql://postgres:PASSWORD@db.PROJECT_REF.supabase.co:5432/postgres?sslmode=require&search_path=public
